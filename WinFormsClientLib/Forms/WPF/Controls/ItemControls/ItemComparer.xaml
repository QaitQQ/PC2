<UserControl x:Class="WinFormsClientLib.Forms.WPF.Controls.ItemControls.ItemComparer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinFormsClientLib.Forms.WPF.Controls.ItemControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid>
            <StackPanel Grid.Column="0" Width="200"  HorizontalAlignment="Left">
                <ItemsControl Name="ButtonStack"></ItemsControl>
            </StackPanel>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Name="ApplyAllButton" Click="ApplyAllButton_Click" Content="ApplyAll"></Button>
            <Button Grid.Column="1" Name="ClearButton" Click="ClearButton_Click" Content="Clear"></Button>
            <DataGrid x:Name="CommonGrid" Grid.Row="1" Grid.ColumnSpan="2" MaxColumnWidth="200" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Tag="{Binding SelectedIndex, ElementName=CommonGrid}" Click="Button_Click"></Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Binding="{Binding Path=Name}"></DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Path=OldRC}"></DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Path=NewRC}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
