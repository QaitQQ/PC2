<UserControl x:Class="MGSol.Panel.OrdersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1060">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border  CornerRadius="1"  BorderThickness="1">
            <StackPanel Grid.Row="0" Orientation="Horizontal"  Margin="2">
                <Button Margin="2" Height="35" Width="35" Content="↺" Background="#FF64B067" FontSize="24" FontFamily="Arial" FontWeight="Bold" Click="Fill_VOrders"  HorizontalAlignment="Left"/>
                <Button Margin="2"  Height="35" Width="35" Content="📝;" Background="BurlyWood" FontSize="24" FontFamily="Arial" FontWeight="Bold" Click="Button_List"/>
                <Grid Height="40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBox Height="35" Width="100" x:Name="SearchBox" ></TextBox>
                    <Button Height="35"  Content="&#128269;"  Grid.Column="1" Click="Search_Click"></Button>
                </Grid>
                <ComboBox HorizontalAlignment="Center"  VerticalAlignment="Center" Margin="2" Height="35" x:Name="StatusBox" SelectionChanged="StatusBox_SelectionChanged"></ComboBox>
                <Label Content="Акты:" VerticalAlignment="Center"></Label>
                <Border  Height="35" BorderBrush="Green" Background="Gainsboro"  BorderThickness="1" CornerRadius="3" >
                    <StackPanel Orientation="Horizontal">
                        <ItemsControl x:Name="PrintActBtnStack">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Margin="2" Content="{Binding Name}" Click="GetActClick"></Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
        <Button HorizontalAlignment="Right" Content="+" Height="35" Background="Green" FontSize="22" FontFamily="Arial" FontWeight="Bold" Click="Browser_hide"/>
        <Grid x:Name="grid"  Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="StackColumn"/>
                <ColumnDefinition x:Name="BCol" Width="1"/>
            </Grid.ColumnDefinitions>
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility ="Auto" Visibility="Visible">
                <Border  BorderBrush="#FF514141" BorderThickness="2" CornerRadius="4" Margin="2">
                    <StackPanel  Grid.Row="1" >
                        <ItemsControl x:Name="OrderStack" Background="Gainsboro" FontFamily="Arial"  HorizontalAlignment="Left"  >
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <HeaderedContentControl Header="{Binding Path=Key}" FontFamily="Arial" >
                                        <ItemsControl ItemsSource="{Binding Path=.}" Background="Gray"  HorizontalAlignment="Left" >
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderBrush="#FF514141" BorderThickness="2" CornerRadius="4" Margin="2" >
                                                        <Grid Margin="2"  HorizontalAlignment="Left" Background="Cornsilk" >
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition/>
                                                                <RowDefinition/>
                                                            </Grid.RowDefinitions>
                                                            <Grid Grid.Row="0">
                                                                <StackPanel HorizontalAlignment="Left" Width="2000">
                                                                    <TextBlock Margin="3" Text="{Binding APISetting.Name}"></TextBlock>
                                                                    <ListBox Grid.Row="1" ItemsSource ="{Binding Items}" MouseDoubleClick="ListBox_MouseDoubleClick"/>
                                                                    <StackPanel Background="DarkSalmon" Margin ="3"  Orientation="Horizontal"  HorizontalAlignment="Left">
                                                                        <TextBlock Margin="3" Text="{Binding Id}"></TextBlock>
                                                                        <TextBlock Margin="3" Text="{Binding Date}"></TextBlock>
                                                                        <TextBlock Margin="3" Text="{Binding DeliveryDate}"></TextBlock>
                                                                        <TextBlock Margin="3" Text="{Binding Status}"></TextBlock>
                                                                        <CheckBox></CheckBox>
                                                                    </StackPanel>
                                                                    <StackPanel  Margin="3">
                                                                        <Button Margin="3" Content="📦" Background="Green"  Click="Ship_Click" HorizontalAlignment="Left"></Button>
                                                                        <Button Margin="3"  Content="🏷" Click="Label_Click" Background="#FFF5C150"  HorizontalAlignment="Left"/>
                                                                    </StackPanel>
                                                                </StackPanel>
                                                            </Grid>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </HeaderedContentControl>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemContainerStyle>
                                <Style>
                                    <Setter Property="Control.Margin" Value="5"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.HeaderTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding APISetting.Name}"></TextBlock>
                                        </DataTemplate>
                                    </GroupStyle.HeaderTemplate>
                                </GroupStyle>
                            </ItemsControl.GroupStyle>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </ScrollViewer>
            <WebBrowser x:Name="Browser" Grid.Column="1" ></WebBrowser>
        </Grid>
    </Grid>
</UserControl>
