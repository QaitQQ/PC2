using Object_Description;

using static NetEnum.Selector;

namespace Client.Class.Net
{
    public class BaseNetClass
    {
        //private readonly FirstSelector BaseSelector;
        //public BaseNetClass() { BaseSelector = FirstSelector.BaseNetClass; }
        //public Dictionaries GetDictionaries() => (Dictionaries)new TCP_Client_GetObj(BaseSelector).Get(SecondSelector.EnumBase.GetDictionaries);
        //public void SetDictionaries(Dictionaries Dictionaries) => new TCP_Client_GetObj(BaseSelector).Get(SecondSelector.EnumBase.SetDictionaries, Dictionaries);
        //public void ManufFromSite() => new TCP_Client_GetObj(BaseSelector).Get(SecondSelector.EnumBase.ManufFromSite);
    }
}